/**
 * Forum Model
 * Representa un foro en la aplicación
 */
const Forum = {
  id: String,
  name: String,
  description: String,
  category: String,
  image: String,
  postsCount: Number,
  membersCount: Number,
  createdAt: String,
  updatedAt: String,
};

export default Forum;
